// ============================================================================
var config = {
  templateStoreRoot: __dirname + '/data/templates',
  editorStoreRoot:   __dirname + '/data/editors',
  pageStoreRoot:     __dirname + '/data/content'
};

// ============================================================================
module.exports = config;
